import ExpiresCache from './cache/expires-cache';
import decorator from './decorator';
import func from './func';

export default {
  ExpiresCache,
  dataCacheDecorator: decorator,
  dataCacheFunc: func
}

